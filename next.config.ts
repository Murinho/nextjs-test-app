import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // You can add custom config here if you need it.
  // This minimal config works great on Vercel.
  reactStrictMode: true,
};

export default nextConfig;
